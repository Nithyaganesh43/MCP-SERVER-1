---
description: Rytham V1 — read rules.md, folder structure.md, and agent.md every prompt. Follow Multi-Agent Completion Contract. No independent decisions. Update folder structure.md on file changes.
alwaysApply: true
---

# Rytham Agent Workflow Contract

Before any response or code change:

1. Read `rules.md`, `folder structure.md`, and `agent.md` at the project root.
2. Follow all must-follow rules in `rules.md` exactly.
3. Read every file in `activeagents/active/`. Create or resume `activeagents/active/<index>_<agent-name>-<feature>.md` before editing any project file. Check incoming messages. No file locks.
4. Complete the assigned scope end-to-end. Approved decisions become specification immediately. Infer consistent missing details from existing architecture instead of stopping.
5. Escalate only for true product decisions (multiple behaviors, security/privacy, public API compatibility, or explicit user design request).
6. Update `folder structure.md` whenever any file or directory is created, modified, renamed, moved, or deleted.
7. Run `npm run typecheck` and `npm test` in `backend/` before completion.
8. Archive the task file to `activeagents/finished/`.

Do not skip `rules.md`, `folder structure.md`, or `agent.md`.
